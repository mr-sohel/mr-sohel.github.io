<section id="experience" class="section">
  <div class="section-inner">
    <span class="section-title-sm">Experience</span>
    <h2 class="section-title">Teaching &amp; leadership</h2>

    <div class="exp-card reveal">
      <div class="exp-header">
        <div>
          <h3 class="exp-role">Competitive Programming Instructor</h3>
          <p class="exp-org">BUBT Competitive Programming Community</p>
        </div>
        <time datetime="2024" class="exp-date">2024 – Present</time>
      </div>
      <ul class="exp-list">
        <li>Mentored 50+ students in data structures, algorithms, and contest strategy — contributing to BUBT fielding competitive teams at ICPC Dhaka Regional</li>
        <li>Designed and delivered weekly training sessions covering graphs, dynamic programming, and advanced data structures</li>
        <li>Authored problem sets and editorial write-ups for intra-university programming contests</li>
      </ul>
      <div class="exp-tags">
        <span class="xtag">Teaching</span>
        <span class="xtag">Algorithms</span>
        <span class="xtag">ICPC Prep</span>
        <span class="xtag">Problem Setting</span>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .exp-card {
    padding: var(--spacing-xxl);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-left: 4px solid var(--color-primary);
    border-radius: var(--rounded-lg);
    box-shadow: var(--shadow-sm);

    @media (max-width: 480px) {
      padding: var(--spacing-xl);
    }
  }

  .exp-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--spacing-lg);
    flex-wrap: wrap;
    margin-bottom: var(--spacing-lg);
  }

  .exp-role {
    font-size: 18px;
    font-weight: 600;
    color: var(--color-ink);
    margin: 0 0 4px;
  }

  .exp-org {
    font-size: 14px;
    color: var(--color-ink-secondary);
    margin: 0;
  }

  .exp-date {
    font-size: 12px;
    font-weight: 600;
    padding: 4px 12px;
    background: var(--color-primary-soft);
    color: var(--color-primary);
    border-radius: var(--rounded-pill);
    white-space: nowrap;
  }

  .exp-list {
    margin: 0 0 var(--spacing-lg);
    padding-left: var(--spacing-xl);
    display: flex;
    flex-direction: column;
    gap: 10px;

    li {
      font-size: 14px;
      color: var(--color-ink-secondary);
      line-height: 1.7;
    }
  }

  .exp-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .xtag {
    font-size: 12px;
    font-weight: 500;
    padding: 4px 10px;
    background: var(--color-primary-soft);
    color: var(--color-primary);
    border-radius: var(--rounded-pill);
  }
</style>
