<section id="about" class="section">
  <div class="section-inner">
    <span class="section-title-sm">About</span>
    <h2 class="section-title">Engineer by mindset, competitor by training</h2>

    <div class="about-grid">
      <div class="about-text reveal">
        <p>
          I'm a final-year CSE student at BUBT who believes the best way to
          learn systems is to build them — and the best way to stay sharp is
          to compete. I've spent years solving <strong>1500+ algorithmic
          problems</strong>, competing in <strong>3 ICPC Dhaka Regionals</strong>,
          and shipping projects that span containerized backends, computer vision
          pipelines, and deep learning research.
        </p>
        <p>
          Right now I'm focused on <strong>C# / .NET</strong> and backend
          infrastructure. I'm looking for a Software Engineering role where I
          can apply my problem-solving foundation to production systems that
          matter.
        </p>
      </div>

      <div class="about-stats reveal">
        <div class="about-stat">
          <span class="about-stat-value">3.52</span>
          <span class="about-stat-label">CGPA / 4.00</span>
        </div>
        <div class="about-stat">
          <span class="about-stat-value">3×</span>
          <span class="about-stat-label">ICPC Dhaka Regional</span>
        </div>
        <div class="about-stat">
          <span class="about-stat-value">5+</span>
          <span class="about-stat-label">Years in CP</span>
        </div>
        <div class="about-stat">
          <span class="about-stat-value">89%</span>
          <span class="about-stat-label">Thesis Accuracy</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .about-grid {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: var(--spacing-huge);
    align-items: start;

    @media (max-width: 768px) {
      grid-template-columns: 1fr;
      gap: var(--spacing-xxl);
    }
  }

  .about-text {
    p {
      font-size: 17px;
      line-height: 1.8;
      color: var(--color-ink-secondary);
      margin: 0 0 var(--spacing-xl);

      &:last-child { margin-bottom: 0; }
    }

    strong {
      color: var(--color-ink);
      font-weight: 600;
    }
  }

  .about-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-md);
  }

  .about-stat {
    padding: var(--spacing-lg);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--rounded-lg);
    text-align: center;
  }

  .about-stat-value {
    display: block;
    font-size: 28px;
    font-weight: 700;
    letter-spacing: -0.5px;
    color: var(--color-primary);
    line-height: 1.1;
  }

  .about-stat-label {
    display: block;
    font-size: 12px;
    font-weight: 500;
    color: var(--color-ink-mute);
    margin-top: 4px;
  }
</style>
