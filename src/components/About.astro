<section id="about" class="section">
  <div class="section-inner">
    <span class="section-title-sm">About</span>
    <h2 class="section-title">Engineer by mindset, competitor by training</h2>

    <div class="about-grid">
      <div class="about-text reveal">
        <p>
          I'm <strong>Sohel Rana</strong> (also known as <strong>Mr Sohel</strong> online) — a final-year <strong>CSE student at BUBT</strong> with a deep passion for algorithms and problem-solving. Through competitive programming at the national level, I've solved <strong>1500+ algorithmic problems</strong>, competed in <strong>3 ICPC Dhaka Regionals</strong>, and participated in <strong>10+ national IUPCs</strong>.
        </p>
        <p>
          I love taking that same analytical rigor and applying it to real-world software. I build diverse systems ranging from an in-development competitive programming judge to real-time computer vision models. I'm currently looking for an <strong>entry-level Software Engineering role</strong> where I can use my strong foundation in computer science to tackle complex production challenges.
        </p>
      </div>

      <div class="about-stats reveal">
        <div class="about-stat">
          <span class="about-stat-value">3×</span>
          <span class="about-stat-label">ICPC Dhaka Regional</span>
        </div>
        <div class="about-stat">
          <span class="about-stat-value">Specialist</span>
          <span class="about-stat-label">on Codeforces</span>
        </div>
        <div class="about-stat">
          <span class="about-stat-value">3</span>
          <span class="about-stat-label">End-to-End Systems</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .about-grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: var(--spacing-xxl);
    align-items: start;

    @media (max-width: 820px) {
      grid-template-columns: 1fr;
    }
  }

  .about-text {
    font-size: 16px;
    line-height: 1.8;
    color: var(--color-ink-secondary);
    border-left: 3px solid var(--color-primary);
    padding-left: var(--spacing-md);

    p {
      margin: 0 0 var(--spacing-md);

      &:last-child { margin-bottom: 0; }

      @media (max-width: 480px) {
        font-size: 15px;
      }
    }

    strong {
      color: var(--color-ink);
      font-weight: 600;
      padding: 0 2px;
    }
  }

  .about-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-md);

    @media (max-width: 640px) {
      grid-template-columns: 1fr;
    }
  }

  .about-stat {
    padding: var(--spacing-lg);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--rounded-lg);
    text-align: center;
  }

  .about-stat-value {
    display: block;
    font-size: 36px;
    font-weight: 700;
    letter-spacing: -0.5px;
    color: var(--color-primary);
    line-height: 1.1;
    font-variant-numeric: tabular-nums;
  }

  .about-stat-label {
    display: block;
    font-size: 12px;
    font-weight: 500;
    color: var(--color-ink-mute);
    margin-top: 4px;
  }
</style>
